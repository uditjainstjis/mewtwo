# Experiment Execution Checklist

> **⚠️ SIMULATION WARNING:** All metrics in `results_db.jsonl` were generated by
> `get_simulated_metric()` in `src/eval/run_eval.py`. No real LLM inference was
> performed. The adapters in `src/adapters/registry.py` are random `torch.randn`
> matrices, NOT trained LoRA weights. The router uses keyword heuristics, NOT a
> real CoT classifier. **None of the numbers below constitute experimental evidence.**

## Phase 1: File Generation & Scaffolding
- [x] Create directory structure (`src`, `configs`, `scripts`, `data`)
- [x] Write Prompt Builder (`src/data/build_prompts.py`)
- [x] Write Adapter Registry (`src/adapters/registry.py`)
- [x] Write Adaptive LoRA Layer (`src/adapters/adaptive_multi_lora_linear.py`)
- [x] Write CoT Router Placeholder (`src/routers/cot_router.py`)
- [x] Write Evaluation Loop (`src/eval/run_eval.py`)
- [x] Write Metrics Aggregator (`src/eval/metrics.py`)
- [x] Create Initial Config (`configs/uma_experiments.yaml`)
- [x] Create Shell Scripts (`scripts/`)

## Phase 2: Data Generation
- [x] Run `build_prompts.py` to populate `/data/` with pure domain JSONs and mixed domain JSONLs

## Phase 3: Setup Evaluation Env
- [x] Verify `mlx` / `torch` dependencies and physical weights map correctly via `registry.py`

## Phase 4: Phase 0/1 Grid Execution
- [x] Run Sanity Checks (`exp_1_baseline`, `exp_5_adaptive_05`) via `scripts/run_phase1_proof.sh`
- [x] Check `results_db.jsonl` for correct formatting

## Phase 5: Phase 2 Full Grid Execution
- [x] Exp #1–#3 on mixed datasets
- [x] Exp #4–#7 on mixed datasets
- [x] Full grid on pure domains
- [x] Full grid on general ability (WikiText, MMLU-General)
- [x] Run `metrics.py` summarizer
- [x] Insert data into paper tables

## Final Outcome (SIMULATION ONLY)
**These numbers are from `get_simulated_metric()`, NOT from real model inference.**
Simulated Composition Gain: 0.077, Simulated Gen Preserv: 8.939%.
Pipeline scaffolding verified. Real model integration is required before any claims.

## Phase 6: Real Model Integration (NOT STARTED)
- [ ] Replace `get_simulated_metric()` with `compute_metric()` on real model output
- [ ] Replace `prediction = "def black_scholes(): pass"` with `generate_response()` calling a real backend
- [ ] Replace `torch.randn` adapter matrices with actual trained LoRA weights
- [ ] Run first real single-prompt sanity check and paste logs
- [ ] Run minimal real grid and collect genuine `results_db.jsonl`